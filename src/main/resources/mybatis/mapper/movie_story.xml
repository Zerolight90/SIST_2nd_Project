<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="movieStory">

    <select id="getReviews" parameterType="long" resultType="mybatis.vo.MovieStoryVO">
        SELECT 'images/movie_poster_default.jpg' AS posterUrl, m.name AS title, r.reviewContent AS comment
        FROM review r
                 JOIN movie m ON r.mIdx = m.mIdx
        WHERE r.userIdx = #{userIdx} AND r.reviewStatus = 0
    </select>

    <select id="getWatchedMovies" parameterType="long" resultType="mybatis.vo.MovieStoryVO">
        SELECT 'images/movie_poster_default.jpg' AS posterUrl, m.name AS title
        FROM watched_movie wm
                 JOIN movie m ON wm.mIdx = m.mIdx
        WHERE wm.userIdx = #{userIdx}
    </select>

    <select id="getFavoriteMovies" parameterType="long" resultType="mybatis.vo.MovieStoryVO">
        SELECT 'images/movie_poster_default.jpg' AS posterUrl, m.name AS title
        FROM favorite_movie fm
                 JOIN movie m ON fm.mIdx = m.mIdx
        WHERE fm.userIdx = #{userIdx}
    </select>

</mapper>