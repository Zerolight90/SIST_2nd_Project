<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="movieStory">

    <select id="getReviews" parameterType="Long" resultType="mybatis.vo.MovieStoryVO">
        SELECT m.poster AS posterUrl,
               m.name AS title,
               r.reviewContent AS comment,
               r.mIdx AS mIdx
        FROM review r
                 JOIN movie m ON r.mIdx = m.mIdx
        WHERE r.userIdx = #{userIdx} AND r.reviewStatus = 1
    </select>

    <select id="getWatchedMovies" parameterType="java.util.Map" resultType="mybatis.vo.MovieStoryVO">
        SELECT m.poster AS posterUrl, m.name AS title, wm.mIdx
        FROM watched_movie wm
                 JOIN movie m ON wm.mIdx = m.mIdx
        WHERE wm.userIdx = #{userIdx}
        ORDER BY wm.watchedIdx DESC
            LIMIT #{offset}, #{numPerPage}
    </select>

    <select id="getWatchedCount" parameterType="Long" resultType="int">
        SELECT COUNT(*)
        FROM watched_movie
        WHERE userIdx = #{userIdx}
    </select>

</mapper>