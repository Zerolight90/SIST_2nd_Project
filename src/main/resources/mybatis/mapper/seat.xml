<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="seat">
    <select id="reserveSeat" resultType="mybatis.vo.SeatVO" parameterType="String">
        SELECT s.seatIdx, s.seatNumber FROM seat s
        INNER JOIN seat_status ss
        on s.seatIdx = ss.seatIdx
        AND sIdx = #{sIdx}
        AND seatStatus = 1;
    </select>
</mapper>