<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="log">
    <!-- 모든 로그 기록을 얻어내는 쿼리 (종민) -->
    <select id="getAllLog" resultType="mybatis.vo.LogVO">
        select * from log
    </select>
</mapper>